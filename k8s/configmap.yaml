apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: rag-ai-agent
data:
  # IMPORTANT: Update this with your actual external domain(s) after deployment
  # Example: "https://your-domain.com,https://www.your-domain.com"
  # For local testing: "http://localhost,http://localhost:80"
  ALLOWED_ORIGINS: "http://localhost,http://localhost:80"
