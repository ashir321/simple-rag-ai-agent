# Backend API URL (Optional)
# Only needed for local development (npm run dev)
# In production (Docker), nginx proxy handles routing
# Default for dev: http://localhost:8000
# Default for production: empty (uses relative URLs)
VITE_BACKEND_URL=http://localhost:8000
